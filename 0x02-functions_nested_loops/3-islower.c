#include "main.h"

/**
 * islower - checks for lowecase
 *
 * @alpha: takes in alphabet alpha
 *
 * Returns: returns 0 for success run
 *
 */

int _islower(int alpha)
{
	if((alpha > 97) && (alpha < 122))
		return (1);
	else
		return (0)
}
